# TODO - Student dashboard event registration

- [ ] Confirm required UI section and scripts will be placed correctly inside `views/student-dashboard.ejs`.
- [ ] Remove the duplicate/incorrect insertion (if present) and integrate the event registration section once.
- [ ] Ensure event scripts don’t break existing modal/shownSection JS.
- [ ] Verify `refreshEvents()` calls `/events` and registration calls `POST /events/:id/register`.
- [ ] Run quick server start/test (npm test/manual) to ensure no template/script issues.

